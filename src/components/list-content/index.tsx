import './styles.scss';
import {ListMessage} from '../listMessage';

export function ListContent() {
    return (
        <section className="listContent">
            <ListMessage />
        </section>
    );
}